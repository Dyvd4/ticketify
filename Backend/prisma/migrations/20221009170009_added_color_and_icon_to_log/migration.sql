BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Log] ADD [color] NVARCHAR(50),
[icon] NVARCHAR(50);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
