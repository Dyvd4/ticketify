version: "1"
services:
    api:
      volumes:
        - type: bind
          source: .\Backend\src
          target: /app/src
        - /app/node_modules
    client:
      volumes:
        - type: bind
          source: .\Client\src
          target: /app/src
        - /app/node_modules
