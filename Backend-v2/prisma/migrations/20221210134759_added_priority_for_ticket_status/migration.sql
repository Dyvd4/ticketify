BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TicketStatus] ADD [priority] INT IDENTITY(1,1);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
